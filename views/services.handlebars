<h1>{{title}}</h1>

{{! Loop services array of objects }}
{{#each services}}
    <div class="card">
        <h2>{{this.name}}</h2>
        <p>Price: {{this.price}}</p>
    </div>
{{/each}}

<p class="small">Items listed: {{itemsTotal}}</p>